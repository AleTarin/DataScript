program myProgram:
  var resultado: int;

  function factorial(n: int) {
    resultado = resultado * n;
    n = n-1;
    if(n>0){
      factorial(n);
    }
  }

  main {
    resultado = 1;
    factorial(10);
    print(resultado);
  }
  end