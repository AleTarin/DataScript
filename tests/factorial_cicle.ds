program myProgram:
  var n, resultado: int;
  
  main {
    readline("N:", n);
    resultado = 1;
    while(n>0){
      resultado = resultado * n;
      n = n - 1;
    }
    print(resultado);
  }
  end