program myProgram:
  var a, b, c[20], n[3][3], m: float; var d, e: bool; var s: string;
  main {
    d = true;
    a = 0;
    while ( d ) {
      print(a);
      print(d);
      a = a + 3;
      if (a>10) {
        d = false;
      }
    }
    readline("a", a);
  }
  end