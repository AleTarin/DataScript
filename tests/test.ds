program myProgram:
  var a, b, c: float; var d, e: bool; var s: string;

  function fun (a:int, b:int) : int {
    var s: string;
    b = 10;
    a = b * 20;
  }

  function fun2 (a:float, b:float, d: string) : float {
    var x: string;
    a = 10.0f;
    a = a * 20.0f;
  }

  function fun3 (a:float, b:float, d: float) : float {
    var x: string; var s, e: bool;
    d = 10.0f;
    a = b * 20.0f;
  }

  main {
    b = 10f;
    a = b + 200.0f;
    d = a > b;
    e = a < c;
    s = "a" + a;
    if (d == e) {
      c = a * a * 2;
    } else {
      c = a;
    }
    while ( d && true ) {
      a = a + 1;
    }
    fun2(a,b,c);
    print(a+c);
    readline("a" + "a", a);
  }
  end